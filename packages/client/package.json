{
  "name": "@bug-game/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm --prefix ../shared run build",
    "dev": "vite",
    "prebuild": "npm --prefix ../shared run build",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@babylonjs/core": "^7.54.2",
    "@bug-game/shared": "^0.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
